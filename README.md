This project demonstrates how to build a simple Linear Regression model using Python and scikit-learn, train it on sample data, and make predictions using the predict() method.

Although Linear Regression is mainly used for regression problems (predicting continuous values), this example shows the basic workflow for training and prediction.
